#!/bin/bash
set -e

echo "Running post-start setup for devcontainer..."
devcontainer-dynamic-config
devcontainer-env

echo "Configuring Git for merge request workflow..."
git config --global push.default simple
git config --global push.autoSetupRemote true
git config --global alias.mr '!devcontainer-git-mr'
